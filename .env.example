# Translation queue: concurrent workers for execution_mode=queued (in-process, >=1).
OWLANGS_TRANSLATION_QUEUE_WORKERS=1

# Stashed translation outputs: retention in days (on-disk, first-download timestamp; not intermediate data).
OWLANGS_RESULT_STASH_RETENTION_DAYS=7
