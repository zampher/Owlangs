import 'package:flutter/material.dart';

class AnonymizeScreen extends StatelessWidget {
  const AnonymizeScreen({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: const Text('Anonymization'),
        ),
        body: const Center(
          child: Text('Anonymization Screen - Coming Soon'),
        ),
      );
}
