default_params = {
    "thinking": "default",
    "chunk_size": 0,  # 0 means unset, will be loaded from user settings
    "concurrent": 10,
    "temperature": 0.3,
    "timeout": 30,
    "retry": 5
}
